---
import LogoSmall from "./LogoSmall.astro";

export interface Props {
  showIndexLink?: boolean;
  showPresentationLink?: boolean;
  showHoursLink?: boolean;
  showMenuLink?: boolean;
  showDayMenuLink?: boolean;
  showProgramationLink?: boolean;
  showContactLink?: boolean;
}

const {
  showIndexLink,
  showPresentationLink,
  showHoursLink,
  showMenuLink,
  showDayMenuLink,
  showProgramationLink,
  showContactLink,
} = Astro.props;
---

<aside hidden md:block h-full>
  <ul h-full flex>
    {
      showIndexLink && (
        <li text-secondary-blue bg-primary>
          <a
            href="/"
            flex="~ col"
            justify-between
            items-center
            class="nav-link"
          >
            <LogoSmall class="w-10 h-10 -translate-x-0.5" />

            <span class="rotated-text">Accueil</span>
          </a>
        </li>
      )
    }

    {
      showPresentationLink && (
        <li text-primary bg-secondary-blue>
          <a href="/presentation" class="nav-link rotated-text">
            Présentation
          </a>
        </li>
      )
    }

    {
      showHoursLink && (
        <li text-primary bg-secondary-yellow>
          <a href="/horaires" class="nav-link rotated-text">
            Horaires
          </a>
        </li>
      )
    }

    {
      showMenuLink && (
        <li text-primary bg-secondary-gray>
          <a href="/menu" class="nav-link rotated-text">
            Carte
          </a>
        </li>
      )
    }

    {
      showDayMenuLink && (
        <li text-primary bg-secondary-beige>
          <a href="/menu-du-jour" class="nav-link rotated-text">
            Menu du jour
          </a>
        </li>
      )
    }

    {
      showProgramationLink && (
        <li text-primary bg-secondary-blue>
          <a href="/programmation" class="nav-link rotated-text">
            Programmation
          </a>
        </li>
      )
    }

    {
      showContactLink && (
        <li text-primary bg-secondary-yellow>
          <a href="/contact" class="nav-link rotated-text">
            Contact
          </a>
        </li>
      )
    }
  </ul>
</aside>

<style>
  .rotated-text {
    writing-mode: vertical-rl;
    transform: rotate(-180deg);
    font-weight: 600;
    text-transform: uppercase;
    font-size: 1.25rem;
  }

  .nav-link {
    height: 100%;
    padding: 1rem 0.625rem;
  }
</style>
